---
title: "Lite Report"
output: pdf_document
params:
  spec: NA
---

```{r}
# The `params` object is available in the document.
params$spec
```

A plot of `params$spec` random points.

```{r}
plot(rnorm(params$spec), rnorm(params$spec))
# ggplot(data.frame(x = rnorm(100),y =  params$spec*rnorm(100)), aes(x = x, y = y)) + geom_point()
```